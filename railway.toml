[build]
builder = "NIXPACKS"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar target/parking-system-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"